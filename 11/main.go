package main

func main() {
	/*var firstBunch = []int{1, 2, 3, 4}
	var secondBunch = []int{3, 4, 5, 6}
	var final []int

	for _, v := range firstBunch {
		for _, k := range secondBunch {
			if v == k {
				final = append(final, v)

			}
		}
	}

	fmt.Println(final) */

	/*	var firstBunch = []int{1, 2, 3, 4}
		var secondBunch = []int{3, 4, 5, 6}
		var final []int

		secondBunchMap := make(map[int]bool)

		for _, k := range secondBunch {
			secondBunchMap[k] = true
		}

		for _, v := range firstBunch {
			if secondBunchMap[v] {
				final = append(final, v)
			}
		}

		fmt.Println(final)*/
}
